# MCP configuration files (may contain sensitive tokens)
# Frontend Vite folder (local only)
frontendvite/

.env
# Allow .env.example files to be tracked (they're example templates, not secrets)
!**/.env.example

# Claude and Cursor IDE configuration (local only)
.claude/
.cursor/

# Temporary files
nul
tmpclaude-*

# Demo and presentation materials (local only)
DEMO_SCRIPT.md
ELEVATOR_PITCH.md

GenAIPromptHelp.md